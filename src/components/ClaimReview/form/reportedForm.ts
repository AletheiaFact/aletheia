const reportedForm = {
  classification: {
      label: "Review this claim",
      type: "select",
      placeholder: "Choose a classification",
      defaultValue: "",
      rules: {
          required: true,
      },
  },
};

export default reportedForm;