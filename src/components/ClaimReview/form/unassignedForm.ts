const unassignedForm = {
    userId: {
        label: "",
        type: "inputSearch",
        placeholder: "claimReviewForm:assignUser",
        defaultValue: "",
        rules: {
            required: true,
        },
    }
};

export default unassignedForm
