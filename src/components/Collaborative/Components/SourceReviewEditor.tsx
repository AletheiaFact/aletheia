import React from "react";
import SourceEditorButton from "./SourceEditorButton";

const SourceReviewEditor = (props) => {
    return <SourceEditorButton {...props} />;
};

export default SourceReviewEditor;
